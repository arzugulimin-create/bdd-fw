/**
 * TestGeneratorAgent — placeholder for the test generator agent.
 *
 * Minimal stub: logs its action and returns a short status string.
 */
export class TestGeneratorAgent {
  generate(): string {
    console.log('TestGeneratorAgent: generating BDD tests...');
    return 'Tests generated';
  }
}
