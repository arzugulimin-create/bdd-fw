import { EnvironmentConfig } from './index';

/** Production environment. Replace the URL with your real production application. */
export const prod: EnvironmentConfig = {
  name: 'prod',
  baseUrl: 'https://www.example.com',
  apiBaseUrl: 'https://www.example.com/api',
};
